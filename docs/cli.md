# CLI

orbit --help
orbit runserver --host 0.0.0.0 --port 8000
orbit migrate
orbit doctype-generate "<Doctype Name>"

markdown
Copy code

> The CLI auto-configures Django (`DJANGO_SETTINGS_MODULE`) and `django.setup()`.
