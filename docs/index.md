# Orbit

Meta-driven DocType framework on Django + DRF.

- **Bootstrap:** one-time workspace creator.
- **CLI:** run server, migrate, generate from DocType.
- **OpenAPI:** `/schema` (Swagger), `/schema/redoc` (ReDoc).
